# Your list of strings
string_list = ["apple, red", "banana, yellow", "grape, purple"]

# Remove everything after the comma in each string
result_list = [s.split(',')[0].strip() for s in string_list]

# Display the result
print(result_list)
