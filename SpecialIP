df[parent_asset_col] = df[parent_asset_col].astype("string").str.strip()

# Replace blank / NaN Asset_Class with Parent_Asset_Class
df[asset_col] = np.where(
    df[asset_col].isna() | (df[asset_col] == "") | (df[asset_col].str.upper() == "NAN"),
    df[parent_asset_col],
    df[asset_col]
)
