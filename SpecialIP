# Remove commas (thousands separators) before conversion
work[C_LOSS] = (
    work[C_LOSS]
    .astype(str)
    .str.replace(",", "", regex=False)
    .pipe(pd.to_numeric, errors="coerce")
    .fillna(0.0)
)
