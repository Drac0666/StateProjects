import pandas as pd

# Assuming your Intex DataFrame is named 'intex_df'

# Select only the required columns from intex_df and add the prefix
intex_columns = ['Deal/Tranche ID', 'Tranche Currency', 'Deal Name', 'Deal Type', 'Country', 'Group Collateral Type']
intex_df_filtered = intex_df[intex_columns].add_prefix('Intex_')

# Perform a left join on the 'Deal/Tranche ID' column (assuming it's the key column)
merged_df = pd.merge(filtered_df, intex_df_filtered, left_on='Deal/Tranche ID', right_on='Intex_Deal/Tranche ID', how='left')

# If you want to remove the duplicate 'Deal/Tranche ID' column after the merge
merged_df = merged_df.drop(columns=['Intex_Deal/Tranche ID'])

# Display the merged DataFrame
print(merged_df)
