import numpy as np

# Set a random seed
seed_value = 42
np.random.seed(seed_value)

# Get the random state
state = np.random.get_state()

# Extract the seed (first value of the first tuple in the state)
retrieved_seed = state[1][0]

print("Retrieved Seed:", retrieved_seed)
