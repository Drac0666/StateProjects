import pandas as pd

# Assuming your Limits DataFrame is named 'limits_df'

# Multiply the 'Limit' column by a billion (1e9)
limits_df['Limit'] = limits_df['Limit'] * 1e9

# Perform a left join on the 'IntexAssetClass' column
final_df = pd.merge(merged_df, limits_df[['IntexAssetClass', 'Limit']], on='IntexAssetClass', how='left')

# Display the final DataFrame with the merged Limit column
print(final_df)
