import pandas as pd

def merge_dataframes_for_ticket(dict_of_dfs, ticket):
    # Initialize an empty DataFrame to hold the results
    result_df = pd.DataFrame()

    # Loop through the dictionary
    for date, df in dict_of_dfs.items():
        # Filter the DataFrame for the specific ticket
        filtered_df = df[df['Ticket'] == ticket]
        
        # Add the date as a new column in the DataFrame
        filtered_df['Date'] = date
        
        # Append to the result DataFrame
        result_df = pd.concat([result_df, filtered_df])

    # Set the 'Date' column as the index
    result_df.set_index('Date', inplace=True)
    
    # Sort the DataFrame by the index (dates)
    result_df.sort_index(inplace=True)

    # Select and order the columns as required
    result_df = result_df[['Ticket', 'Fair Price', 'par Value']]
    
    return result_df

# Example usage
# Assuming 'dataframes_dict' is your dictionary of DataFrames and 'TICKET123' is the specific ticket you are looking for
# merged_df = merge_dataframes_for_ticket(dataframes_dict, 'TICKET123')
# print(merged_df)
