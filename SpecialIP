def reorder_columns(df):
    """
    Reorders the DataFrame so that the last columns become the first ones.

    Parameters:
    df (pd.DataFrame): Input DataFrame.

    Returns:
    pd.DataFrame: Reordered DataFrame.
    """
    reordered_columns = ['Key'] + df.columns[1:][::-1].tolist()
    return df[reordered_columns]
