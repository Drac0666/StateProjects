import pandas as pd

# Assuming you already have a DataFrame `df` with the columns `parvalue` and `FXRate`

# Calculate ParValue [USD] based on the condition
df['ParValue [USD]'] = df.apply(
    lambda row: row['parvalue'] * row['FXRate'] if row['FXRate'] >= 1 else row['parvalue'] / row['FXRate'], axis=1
)

# Now, df will contain the new 'ParValue [USD]' column with the calculated values
