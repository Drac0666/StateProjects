import os

# Specify the current and new file names
current_file_name = "maciek123"
new_file_name = "maciek123456"

# Construct the full paths for the current and new file names
current_path = os.path.join(os.getcwd(), current_file_name)
new_path = os.path.join(os.getcwd(), new_file_name)

try:
    # Rename the first file
    os.rename(current_path1, new_path1)
    print(f"File '{current_file_name1}' has been renamed to '{new_file_name1}'.")

    # Rename the second file
    os.rename(current_path2, new_path2)
    print(f"File '{current_file_name2}' has been renamed to '{new_file_name2}'.")
except FileNotFoundError as e:
    print(f"File not found: {e}")
except FileExistsError as e:
    print(f"New file name already exists: {e}")
except Exception as e:
    print(f"An error occurred: {e}")
